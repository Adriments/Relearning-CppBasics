/*
#include <iostream>

using namespace std;

void speak(string message) {
	cout << message << endl;
}

void speak(int number) {
	cout << number << endl;
}

void speak(double number) {
	cout << number << endl;
}

double Area(double radius) {
	return 3.14 * radius * radius;
}

double Area(double base, double height) {
	int area = (base * height) / 2;
	return area;
}

int main() {
	
	//Overloading functions - multiple versions of the same function
	//Do this by changing the parameter. CANT HAVE SAME PARAMETERS.
	speak("Hi I like shirts");
	speak(45);
	speak(34.5);

	cout << "Area of my Circle " << Area(5) << endl;
	cout << "Area of my Triangle " << Area(2, 3) << endl;

	return 0;
} */